# Zsh keybindings configuration

# Fast directory navigation with sam-finder (ctrl+f)
bindkey -s '^f' "zsf\n"

# Package.json script runner with fzf (ctrl+s)
bindkey '^s' undefined
bindkey -s '^s' "run_scripts\n"